local someStr,someInt,idiv = "",1 in
    local idiv = fun (x:integer):((nil,string)|(integer, integer)) return nil,"a" in
        local p,q = idiv(1) in
            if p then someInt = q + 2 else someStr = q .. "a"